# Copy to .env. Server-only secrets — never sent to the browser.
CLERK_SECRET_KEY=sk_test_...
# Postgres. Leave unset to run without saving games/ratings.
DATABASE_URL=postgres://user:pass@host:5432/chess
# Optional overrides:
# PORT=3001
# CLIENT_ORIGIN=http://localhost:5173
