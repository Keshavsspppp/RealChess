# Copy to .env.local. Exposed to the browser (safe — publishable key is public).
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...
# Backend URL. Default is http://localhost:3001 if unset.
# VITE_SERVER_URL=http://localhost:3001
